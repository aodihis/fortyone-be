services:
  - name: axum-app
    type: web
    runtime: rust
    region: oregon
    plan: free
    buildCommand: "cargo build --release"
    startCommand: "./target/release/fortyone-be"
    envVars:
      - key: SERVER_ADDRESS
        value: "127.0.0.1:3000"